@model IEnumerable<AccueilHera.Models.Abonnement>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nom_Abonnement)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description_Abonnement)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prix_Abonnement)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Statut_Abonnement)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nom_Abonnement)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description_Abonnement)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Prix_Abonnement)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Statut_Abonnement)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Abonnement }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Abonnement }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Abonnement })
        </td>
    </tr>
}

</table>
